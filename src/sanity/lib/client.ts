/**
 * Sanity client - Stub implementation for build compatibility
 * Replace with actual Sanity client configuration when needed
 */

export const client = {
  fetch: async <T = any>(query: string, params?: any): Promise<T | null> => {
    // Stub implementation
    return null;
  },
};

